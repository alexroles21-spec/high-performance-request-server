version: '3.8'

services:
  smart-proxy-server:
    image: alpine:latest
    container_name: proxy-request-service
    restart: always
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PROXY_MODE=high-performance

